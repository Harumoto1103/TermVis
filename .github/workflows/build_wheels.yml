name: Build and Publish

on:
  push:
    branches: [ master, main ]
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      # Linux build: High-performance environment with AlmaLinux 8 (manylinux_2_28)
      - name: Build wheels (Linux)
        if: runner.os == 'Linux'
        uses: PyO3/maturin-action@v1
        env:
          LIBCLANG_PATH: /usr/lib64
        with:
          command: build
          args: --release --out dist
          manycolor: 2_28
          before-script-linux: |
            dnf install -y clang-devel llvm-devel opencv-devel

      # Windows build: Utilizing vcpkg for reliable OpenCV linking
      - name: Install OpenCV (Windows)
        if: runner.os == 'Windows'
        run: |
          vcpkg install opencv4:x64-windows
          vcpkg integrate install

      - name: Build wheels (Windows)
        if: runner.os == 'Windows'
        uses: PyO3/maturin-action@v1
        env:
          VCPKG_ROOT: C:/vcpkg
        with:
          command: build
          args: --release --out dist

      # macOS build: Handling Homebrew paths for libclang and OpenCV
      - name: Install Dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install opencv llvm
          echo "LIBCLANG_PATH=$(brew --prefix llvm)/lib" >> $GITHUB_ENV

      - name: Build wheels (macOS)
        if: runner.os == 'macOS'
        uses: PyO3/maturin-action@v1
        with:
          command: build
          args: --release --out dist

      - name: Upload built wheels as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.os }}
          path: dist

  publish:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    if: "startsWith(github.ref, 'refs/tags/v')"
    needs: [build_wheels]
    steps:
      - name: Download all built wheels
        uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          merge-multiple: true
          path: dist

      - name: Upload to PyPI
        uses: PyO3/maturin-action@v1
        env:
          MATURIN_PYPI_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        with:
          command: upload
          args: --non-interactive --skip-existing dist/*
